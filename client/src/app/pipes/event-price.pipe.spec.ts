import { EventPricePipe } from './event-price.pipe';

describe('EventPricePipe', () => {
  it('create an instance', () => {
    const pipe = new EventPricePipe();
    expect(pipe).toBeTruthy();
  });
});
